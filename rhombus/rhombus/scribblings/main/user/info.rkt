#lang info

(define scribblings
  ;; has 'doc-properties to indicate that this document add or replaces
  ;;  "rhombus/index.html" from "rhombus.scrbl" when rendered into user space:
  '(("user-rhombus.scrbl" (user-doc depends-all no-depend-on) (omit))))

(define user-doc-index "rhombus/scribblings/main/user")
