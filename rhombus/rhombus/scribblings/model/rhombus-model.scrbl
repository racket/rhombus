#lang rhombus/scribble/manual
@(import:
    "common.rhm" open)

@title{Rhombus Language Model}

For a general overview of the Rhombus language, see @docref(guide_doc).
This document defines the evaluation and syntax concepts of the Rhombus
language.
For documentation on the Rhombus bindings that implement these concepts,
see @docref(ref_doc) and @docref(meta_doc).

@table_of_contents()

@include_section("eval-model.scrbl")
@include_section("syntax-model.scrbl")
