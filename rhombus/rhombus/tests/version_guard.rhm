#lang rhombus/static/and_meta
import:
  rhombus/version_meta

export:
  at_least

defn.macro
| 'at_least $vers: $body':
    'version_meta.when_racket_at_least $vers: $body'
| 'at_least $vers | $then | $else':
    'version_meta.if_racket_at_least $vers | $then | $else'
