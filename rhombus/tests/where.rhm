#lang rhombus

check:
  use_static
  def x where x.length() > 1 = "Hello"
  x.length()
  ~is 5

block:
  fun classify(v):
    match v
    | n ::Int where (n mod 2) .= 0: "even"
    | _ :: Int: "odd"
    | ~else "other"
  check:
    classify(0) ~is "even"
    classify(1) ~is "odd"
    classify(#false) ~is "other"

check:
  ~eval
  def x where x.length() > 10 = "Hello"
  ~raises "value does not satisfy annotation"

check:
  ~eval
  block:
    let x where #true = 1
    x
  ~raises "pattern requires early binding of its names"
