#lang info

(define collection 'multi)

(define deps
  '("base"
    "rhombus-lib"
    "draw-lib"))

(define pkg-desc "implementation (no documentation) part of \"rhombus-draw\"")

(define license '(Apache-2.0 OR MIT))

(define version "0.1")
