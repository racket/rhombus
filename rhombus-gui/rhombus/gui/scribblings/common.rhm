#lang rhombus/static

import:
  meta_label:
    rhombus open
    rhombus/gui
    rhombus/draw
    rhombus/gui open
    lib("racket/gui/easy.rkt") open:
      except:
        render

export:
  meta_label:
    all_from(rhombus)
    gui
    draw
    all_from(rhombus/gui)
    all_from(lib("racket/gui/easy.rkt"))
