#lang rhombus
import:
  rhombus/meta open
  scribble/base expose: #{include-section}

export:
  include_section

defn.macro 'include_section($(mod :: Term))':
  '$(parsed(['#{include-section}', mod]))'
