#lang rhombus/static
import:
  "catalog.rhm"
  "installer.rhm"
  "doc.rhm"
  "site.rhm"

fun build(racket_version, rhombus_verion, config):
  catalog.build_catalog(racket_version, rhombus_verion, config)
  installer.build_installer(racket_version, rhombus_verion, config)
  doc.build_docs(racket_version, rhombus_verion, config)
  site.build_site(racket_version, rhombus_verion, config)

module main:
  import "cmdline.rhm"
  cmdline.parse(build)
